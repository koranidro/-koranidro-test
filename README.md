# @koranidro/test

## 1. 사용법:

```javascript
import { add } from "@koranidro/test";

console.log(`sum = ${add(5, 1)}`);
```

또는

```javascript
const { add } = require("@koranidro/test");

console.log(`sum = ${add(5, 1)}`);
```

## 2. 실행 결과:

```bash
sum = 6
```